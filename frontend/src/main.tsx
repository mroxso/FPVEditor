import { useEffect, useMemo, useState } from "react";
import { createRoot } from "react-dom/client";
import { invoke } from "@tauri-apps/api/core";
import { open, save } from "@tauri-apps/plugin-dialog";
import { ChevronDown, ChevronLeft, ChevronRight, Circle, Clapperboard, Command, Download, FolderOpen, Layers2, MoreHorizontal, Pause, Play, Plus, RotateCw, Send, Sparkles, Undo2, Video, WandSparkles, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Separator } from "@/components/ui/separator";
import { Slider } from "@/components/ui/slider";
import { Switch } from "@/components/ui/switch";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group";
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip";
import "./styles.css";

type TrackKind = "Video" | "Audio";
type Profile = { smoothness: number; strength: number; horizon_lock: boolean; dynamic_fov: number };
type Clip = { id: string; source_path: string; in_point: number; out_point: number; position: number; stabilization?: Profile | null; lut_path?: string | null };
type Track = { id: string; kind: TrackKind; name: string; clip_order: string[] };
type Project = { name: string; fps: number; width: number; height: number; tracks: Track[]; clips: Record<string, Clip> };
type Outcome = { project: Project; can_undo: boolean; can_redo: boolean };
const seedProject: Project = { name: "Untitled", fps: 60, width: 1920, height: 1080, tracks: [], clips: {} };
const timecode = (us: number) => `${Math.floor(us / 60_000_000)}:${String(Math.floor(us / 1_000_000) % 60).padStart(2, "0")}`;
const fileName = (path: string) => path.split(/[\\/]/).pop() || path;

function IconButton({ label, children, ...props }: React.ComponentProps<typeof Button> & { label: string }) { return <Tooltip><TooltipTrigger asChild><Button aria-label={label} size="icon-sm" variant="ghost" {...props}>{children}</Button></TooltipTrigger><TooltipContent>{label}</TooltipContent></Tooltip>; }

function App() {
  const [project, setProject] = useState<Project>(seedProject); const [selected, setSelected] = useState<string>(); const [playhead, setPlayhead] = useState(0); const [playing, setPlaying] = useState(false); const [undoable, setUndoable] = useState(false); const [redoable, setRedoable] = useState(false); const [notice, setNotice] = useState("Ready to edit"); const [copilotOpen, setCopilotOpen] = useState(true); const [prompt, setPrompt] = useState(""); const [messages, setMessages] = useState([{ role: "ai", text: "I can help make this flight cut tighter. Ask me to stabilize, trim, or assemble a sequence." }]);
  const selectedClip = selected ? project.clips[selected] : undefined; const duration = useMemo(() => Math.max(30_000_000, ...Object.values(project.clips).map((clip) => clip.position + clip.out_point - clip.in_point)), [project]);
  const apply = (outcome: Outcome) => { setProject(outcome.project); setUndoable(outcome.can_undo); setRedoable(outcome.can_redo); }; const command = async (value: object) => { try { apply(await invoke<Outcome>("execute", { command: value })); } catch (error) { setNotice(String(error)); } }; const refresh = async () => { try { setProject(await invoke<Project>("timeline")); } catch { setNotice("Browser preview — Tauri IPC offline"); } };
  useEffect(() => { void refresh(); }, []); useEffect(() => { if (!playing) return; const timer = window.setInterval(() => setPlayhead((current) => current >= duration ? 0 : current + 100_000), 100); return () => window.clearInterval(timer); }, [playing, duration]);
  const addTrack = async (kind: TrackKind) => command({ command: "add_track", kind, name: `${kind === "Video" ? "V" : "A"}${project.tracks.filter((track) => track.kind === kind).length + 1}` });
  const importMedia = async () => { const path = await open({ multiple: false, filters: [{ name: "Video", extensions: ["mp4", "mov", "mkv", "m4v"] }] }); if (!path || Array.isArray(path)) return; let track = project.tracks.find((item) => item.kind === "Video"); if (!track) { await addTrack("Video"); const latest = await invoke<Project>("timeline"); setProject(latest); track = latest.tracks.find((item) => item.kind === "Video"); } if (track) { await command({ command: "add_clip", track_id: track.id, clip: { source_path: path, in_point: 0, out_point: 10_000_000, position: duration > 30_000_000 ? duration : 0 } }); setNotice(`Imported ${fileName(path)}`); } };
  const saveProject = async () => { const path = await save({ defaultPath: `${project.name || "flight"}.fpv.json`, filters: [{ name: "FPV Project", extensions: ["fpv.json"] }] }); if (path) { await invoke("save_project", { path }); setNotice("Project saved"); } }; const loadProject = async () => { const path = await open({ multiple: false, filters: [{ name: "FPV Project", extensions: ["json"] }] }); if (typeof path === "string") { setProject(await invoke<Project>("load_project", { path })); setSelected(undefined); setNotice("Project loaded"); } };
  const chat = async () => { if (!prompt.trim()) return; const text = prompt; setPrompt(""); setMessages((items) => [...items, { role: "you", text }]); try { const response = await invoke<string>("chat", { prompt: text }); setMessages((items) => [...items, { role: "ai", text: response }]); await refresh(); } catch { setMessages((items) => [...items, { role: "ai", text: "Set up an AI provider in settings, then I can work directly on this timeline." }]); } };
  return <TooltipProvider><main className="min-h-screen min-w-[1120px] bg-background text-foreground selection:bg-foreground selection:text-background"><header className="flex h-14 items-center border-b px-5"><div className="flex w-72 items-center gap-3"><div className="grid size-7 place-items-center rounded-md bg-primary text-primary-foreground"><Video /></div><div><p className="font-heading text-sm font-semibold tracking-tight">FPV Editor</p><p className="text-[10px] uppercase tracking-[0.16em] text-muted-foreground">Desktop cut suite</p></div></div><div className="flex flex-1 items-center justify-center gap-2"><Circle className="size-2 fill-foreground" /><span className="font-mono text-xs">{project.name}</span><span className="text-xs text-muted-foreground">{project.width} × {project.height} · {project.fps} fps</span></div><div className="flex w-72 justify-end gap-1"><Button variant="ghost" size="sm" onClick={() => void loadProject()}><FolderOpen data-icon="inline-start" />Open</Button><Button variant="ghost" size="sm" onClick={() => void saveProject()}><Download data-icon="inline-start" />Save</Button><Button size="sm" onClick={() => setNotice("Export presets are connected to the render pipeline.")}>Export <ChevronRight data-icon="inline-end" /></Button></div></header><nav className="flex h-11 items-center gap-2 border-b px-4"><Button size="sm" onClick={() => void importMedia()}><Plus data-icon="inline-start" />Import media</Button><Separator orientation="vertical" className="mx-1 h-4" /><IconButton label="Undo" disabled={!undoable} onClick={() => invoke<Outcome>("undo").then(apply)}><Undo2 /></IconButton><IconButton label="Redo" disabled={!redoable} onClick={() => invoke<Outcome>("redo").then(apply)}><RotateCw /></IconButton><p className="ml-2 flex-1 text-xs text-muted-foreground">{notice}</p><Button variant={copilotOpen ? "secondary" : "ghost"} size="sm" onClick={() => setCopilotOpen((open) => !open)}><Sparkles data-icon="inline-start" />Copilot</Button></nav><section className={`grid min-h-0 ${copilotOpen ? "grid-cols-[230px_minmax(420px,1fr)_270px_310px]" : "grid-cols-[230px_minmax(420px,1fr)_270px]"}`} style={{ height: "calc(100vh - 315px)" }}><aside className="border-r bg-card"><PanelTitle icon={<Layers2 />} title="Media" action={<IconButton label="Import media" onClick={() => void importMedia()}><Plus /></IconButton>} /><div className="p-3">{Object.values(project.clips).length === 0 ? <EmptyMedia onImport={importMedia} /> : <div className="flex flex-col gap-1">{Object.values(project.clips).map((clip) => <Button key={clip.id} variant={selected === clip.id ? "secondary" : "ghost"} className="h-auto justify-start px-2 py-2" onClick={() => setSelected(clip.id)}><Clapperboard data-icon="inline-start" /><span className="min-w-0 text-left"><span className="block truncate text-xs">{fileName(clip.source_path)}</span><span className="block font-mono text-[10px] text-muted-foreground">{timecode(clip.out_point - clip.in_point)}</span></span></Button>)}</div>}</div></aside><section className="grid min-w-0 grid-rows-[minmax(0,1fr)_56px] bg-muted/30"><div className="grid place-items-center p-6"><div className="relative aspect-video w-[min(76vh,76%)] overflow-hidden rounded-lg border bg-card shadow-2xl"><div className="absolute inset-0 preview-grid" /><div className="absolute inset-[8%] border border-dashed border-border/70" /><div className="absolute left-[-10%] top-1/2 w-[120%] border-t border-foreground/30 -rotate-6" /><div className="absolute inset-0 grid place-items-center text-center"><div><Badge variant="outline" className="mb-3 font-mono text-[10px] uppercase tracking-[0.18em]">Monitor 01</Badge><h1 className="font-heading text-base font-semibold">{selectedClip ? fileName(selectedClip.source_path) : "Select a clip to preview"}</h1><p className="mt-2 font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Rec 709 · video monitor</p></div></div><div className="absolute bottom-3 left-4 right-4 flex justify-between font-mono text-[10px] text-muted-foreground"><span>{timecode(playhead)}</span><span>60 FPS</span></div></div></div><div className="flex items-center justify-center gap-7 border-t"><span className="font-mono text-xs text-muted-foreground">{timecode(playhead)}</span><div className="flex items-center gap-1"><IconButton label="Previous second" onClick={() => setPlayhead(Math.max(0, playhead - 1_000_000))}><ChevronLeft /></IconButton><Button aria-label="Play or pause" size="icon" className="rounded-full" onClick={() => setPlaying((value) => !value)}>{playing ? <Pause /> : <Play />}</Button><IconButton label="Next second" onClick={() => setPlayhead(Math.min(duration, playhead + 1_000_000))}><ChevronRight /></IconButton></div><span className="font-mono text-xs text-muted-foreground">{timecode(duration)}</span></div></section><aside className="border-l bg-card"><PanelTitle icon={<Command />} title="Inspector" action={<IconButton label="More options"><MoreHorizontal /></IconButton>} />{selectedClip ? <ClipInspector clip={selectedClip} command={command} /> : <EmptyInspector />}</aside>{copilotOpen && <Copilot messages={messages} prompt={prompt} setPrompt={setPrompt} chat={chat} close={() => setCopilotOpen(false)} />}</section><Timeline project={project} selected={selected} duration={duration} playhead={playhead} onSelect={setSelected} onAddTrack={addTrack} /></main></TooltipProvider>;
}

function PanelTitle({ icon, title, action }: { icon: React.ReactNode; title: string; action: React.ReactNode }) { return <div className="flex h-11 items-center justify-between border-b px-3"><div className="flex items-center gap-2 text-xs font-medium"><span className="text-muted-foreground">{icon}</span>{title}</div>{action}</div>; }
function EmptyMedia({ onImport }: { onImport: () => Promise<void> }) { return <Card size="sm" className="border-dashed bg-transparent shadow-none"><CardHeader><CardTitle className="text-sm">Start with a flight</CardTitle><CardDescription className="text-xs">Bring in a take, then build the cut below.</CardDescription></CardHeader><CardContent><Button variant="outline" size="sm" className="w-full" onClick={() => void onImport()}><Plus data-icon="inline-start" />Import media</Button></CardContent></Card>; }
function EmptyInspector() { return <div className="grid h-[calc(100%-44px)] place-items-center p-6 text-center"><div><Circle className="mx-auto mb-3 size-7 text-muted-foreground" /><p className="text-sm font-medium">Nothing selected</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Select a clip to tune stabilization, look, and speed.</p></div></div>; }
function ClipInspector({ clip, command }: { clip: Clip; command: (value: object) => Promise<void> }) { const [smoothness, setSmoothness] = useState(clip.stabilization?.smoothness ?? 0.55); const profile = (overrides: Partial<Profile> = {}) => ({ smoothness, strength: clip.stabilization?.strength ?? 1, horizon_lock: clip.stabilization?.horizon_lock ?? true, dynamic_fov: clip.stabilization?.dynamic_fov ?? 0.12, ...overrides }); return <Tabs defaultValue="adjust" className="h-[calc(100%-44px)]"><TabsList variant="line" className="mx-3 mt-3 w-[calc(100%-24px)] justify-start"><TabsTrigger value="adjust" className="text-xs">Adjust</TabsTrigger><TabsTrigger value="effects" className="text-xs">Effects</TabsTrigger></TabsList><div className="flex flex-col gap-5 p-4"><Card size="sm" className="shadow-none"><CardHeader><CardTitle className="truncate text-xs">{fileName(clip.source_path)}</CardTitle><CardDescription className="font-mono text-[10px]">{timecode(clip.out_point - clip.in_point)} · SOURCE</CardDescription></CardHeader></Card><div className="flex flex-col gap-3"><div className="flex items-center justify-between"><p className="text-xs font-medium">Stabilization</p><Switch checked={Boolean(clip.stabilization)} onCheckedChange={(checked) => { if (checked) void command({ command: "apply_stabilization", clip_id: clip.id, profile: profile() }); }} /></div><div className="flex items-center justify-between text-xs text-muted-foreground"><span>Smoothness</span><span className="font-mono">{Math.round(smoothness * 100)}%</span></div><Slider value={[smoothness]} max={1} step={0.05} onValueChange={([value]) => setSmoothness(value)} onValueCommit={() => void command({ command: "apply_stabilization", clip_id: clip.id, profile: profile() })} /><div className="flex items-center justify-between"><span className="text-xs text-muted-foreground">Horizon lock</span><Switch checked={profile().horizon_lock} onCheckedChange={(horizon_lock) => void command({ command: "apply_stabilization", clip_id: clip.id, profile: profile({ horizon_lock }) })} /></div></div><Separator /><div><p className="mb-3 text-xs font-medium">Speed</p><ToggleGroup type="single" defaultValue="1" variant="outline" spacing={0} className="w-full"><ToggleGroupItem value="0.5" className="flex-1 text-xs">0.5×</ToggleGroupItem><ToggleGroupItem value="1" className="flex-1 text-xs">1×</ToggleGroupItem><ToggleGroupItem value="2" className="flex-1 text-xs">2×</ToggleGroupItem></ToggleGroup></div><Button variant="outline" size="sm" onClick={() => void command({ command: "apply_lut", clip_id: clip.id, lut_path: "monochrome.cube" })}>Apply monochrome look <ChevronDown data-icon="inline-end" /></Button></div></Tabs>; }
function Copilot({ messages, prompt, setPrompt, chat, close }: { messages: { role: string; text: string }[]; prompt: string; setPrompt: (value: string) => void; chat: () => Promise<void>; close: () => void }) { return <aside className="grid min-h-0 grid-rows-[44px_1fr_auto_auto] border-l bg-card"><PanelTitle icon={<WandSparkles />} title="Copilot" action={<IconButton label="Close copilot" onClick={close}><X /></IconButton>} /><div className="flex min-h-0 flex-col gap-3 overflow-auto p-4">{messages.map((message, index) => <div key={index} className={message.role === "you" ? "ml-7 rounded-lg bg-primary px-3 py-2 text-xs leading-5 text-primary-foreground" : "mr-4 rounded-lg border bg-muted/50 px-3 py-2 text-xs leading-5"}>{message.text}</div>)}</div><div className="flex flex-wrap gap-1 px-3 pb-2"><Button variant="outline" size="xs" onClick={() => setPrompt("Stabilize the selected clip with horizon lock")}>Stabilize this take</Button><Button variant="outline" size="xs" onClick={() => setPrompt("Find the best moments for a fast freestyle cut")}>Find best moments</Button></div><div className="border-t p-3"><div className="flex gap-2"><Textarea value={prompt} onChange={(event) => setPrompt(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); void chat(); } }} className="min-h-10 resize-none text-xs" placeholder="Ask Copilot to shape the edit…" /><Button aria-label="Send to Copilot" size="icon" onClick={() => void chat()}><Send /></Button></div></div></aside>; }
function Timeline({ project, selected, duration, playhead, onSelect, onAddTrack }: { project: Project; selected?: string; duration: number; playhead: number; onSelect: (id: string) => void; onAddTrack: (kind: TrackKind) => Promise<void> }) { return <section className="relative h-[220px] overflow-hidden border-t bg-card"><div className="grid h-10 grid-cols-[148px_1fr_188px] border-b"><div className="flex items-center gap-2 border-r px-4"><span className="text-xs font-medium">Timeline</span><Badge variant="outline" className="font-mono text-[9px]">MAGNETIC</Badge></div><div className="timeline-ruler relative">{[0, 5, 10, 15, 20, 25, 30].map((second) => <span key={second} className="absolute top-3 font-mono text-[10px] text-muted-foreground" style={{ left: `${second / 30 * 100}%` }}>{`00:${String(second).padStart(2, "0")}`}</span>)}</div><div className="flex items-center justify-end gap-1 px-2"><Button variant="ghost" size="xs" onClick={() => void onAddTrack("Video")}><Plus data-icon="inline-start" />Video</Button><Button variant="ghost" size="xs" onClick={() => void onAddTrack("Audio")}><Plus data-icon="inline-start" />Audio</Button></div></div><div className="relative h-[180px] overflow-auto">{project.tracks.length === 0 && <div className="grid h-full place-items-center text-center"><div><p className="text-sm font-medium">Your timeline is empty</p><p className="mt-1 text-xs text-muted-foreground">Import a take or add a track to begin.</p></div></div>}{project.tracks.map((track) => <div key={track.id} className="grid h-16 grid-cols-[148px_1fr] border-b"><div className="border-r px-4 py-3"><p className="font-mono text-xs">{track.name}</p><p className="mt-1 text-[10px] uppercase tracking-wider text-muted-foreground">{track.kind}</p></div><div className="timeline-lane relative">{track.clip_order.map((id) => { const clip = project.clips[id]; if (!clip) return null; const left = clip.position / duration * 100; const width = Math.max(8, (clip.out_point - clip.in_point) / duration * 100); return <button key={id} onClick={() => onSelect(id)} className={`timeline-clip ${selected === id ? "is-selected" : ""}`} style={{ left: `${left}%`, width: `${width}%` }}><Clapperboard /><span>{fileName(clip.source_path)}</span>{clip.stabilization && <Badge variant="outline">STAB</Badge>}</button>; })}</div></div>)}</div><div className="absolute bottom-0 top-10 w-px bg-foreground" style={{ left: `calc(148px + ${(playhead / duration) * 100}% * (100% - 148px) / 100)` }} /></section>; }
createRoot(document.getElementById("root")!).render(<App />);
